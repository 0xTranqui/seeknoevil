// @ts-nocheck
import {Connect} from "../auth/Connect"

export const Footer = () => {
    return (
        <div className="fixed bottom-4 right-4 z-10">
            <Connect />
        </div>
    );
};
